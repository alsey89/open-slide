---
'@open-slide/core': minor
---

Expand the design-token vocabulary: add `surface`, `muted`, and `border` palette roles, `heading` and `caption` type-scale steps, a base `space` unit, and a `shadow` token. All design fields are now optional — partial designs are filled from defaults on load — and the editor's Design panel exposes the new tokens.
