---
'@open-slide/cli': patch
---

Refresh the scaffolded template: document the full block/layout/token set (including stat/callout/divider, full-bleed/grid/blank, and the expanded design tokens), ship a commented `blocks/index.tsx` starter showing a custom block with a prop schema, and sync the bundled skills.
