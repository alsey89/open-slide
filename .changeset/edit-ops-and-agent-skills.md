---
'@open-slide/core': minor
'@open-slide/cli': minor
---

Add a structured edit-operations library (applyOp/applyOps), a `/__deck/:id` dev-server endpoint that applies ops to deck.json, and rewrite the agent skills for deck.json authoring and editing.
