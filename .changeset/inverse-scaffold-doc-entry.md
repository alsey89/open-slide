---
'@open-slide/core': minor
---

Add a React-free `@open-slide/core/doc` entry for the document model, inverse-ops (`invertOp`/`applyOpWithInverse`) for undo/redo, and layout-aware slide scaffolding (`buildSlide`/`addSlideFromLayout`). `set-deck-title` now accepts an optional title so undo can clear one that was never set.
