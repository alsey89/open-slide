---
'@open-slide/core': minor
---

Add built-in blocks `stat` (big number + label + caption), `callout` (accent/surface/outline pill), and `divider`, plus layouts `full-bleed` (media cover + overlay content), `grid` (auto-fit N-up), and `blank` (single freeform slot). All consume the expanded design tokens and ship editor prop schemas.
