---
'@open-slide/core': minor
---

Export `normalizeDesign`, `getBlockSchema`, and the `PropField` / `PropFieldType` / `BlockPropSchema` types from the public API so custom-block authors can type their prop schemas.
